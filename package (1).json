{
  "name": "sahkaar-seva-backend",
  "version": "1.0.0",
  "description": "Backend API for Sahkaar Seva — captures bookings, worker registrations, and safety events from the frontend, with simple file-based persistence.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
